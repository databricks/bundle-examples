resources:
  clusters:
    analytics_cluster:
      cluster_name: Wanderbricks analytics cluster
      spark_version: 15.4.x-scala2.12
      node_type_id: Standard_DS3_v2
      num_workers: 1
      autotermination_minutes: 30
  secret_scopes:
    platform_secrets:
      name: wanderbricks-platform
