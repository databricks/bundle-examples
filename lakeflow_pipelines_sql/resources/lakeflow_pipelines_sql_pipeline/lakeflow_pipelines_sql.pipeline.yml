resources:
  pipelines:
    lakeflow_pipelines_sql_pipeline:
      name: lakeflow_pipelines_sql_pipeline
      serverless: true
      channel: "PREVIEW"
      catalog: ${var.catalog}
      schema: ${var.schema}
      root_path: "."
      libraries:
        - glob:
            include: transformations/**
