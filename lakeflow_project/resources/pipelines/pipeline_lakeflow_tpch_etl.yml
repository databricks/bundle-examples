# The main pipeline for lakeflow_pipelines_python

resources:
  pipelines:
    pipeline_lakeflow_tpch_etl:
      name: pipeline_lakeflow_tpch_etl
      catalog: ${var.catalog}
      schema: ${resources.schemas.tpch_schema.name}
      serverless: true
      root_path: "../../src/lakeflow_tpch_etl"

      libraries:
        - glob:
            include: ../../src/lakeflow_tpch_etl/transformations/**

      # environment:
      #   dependencies:
      #     # We include every dependency defined by pyproject.toml by defining an editable environment
      #     # that points to the folder where pyproject.toml is deployed.
      #     - --editable ${workspace.file_path}
