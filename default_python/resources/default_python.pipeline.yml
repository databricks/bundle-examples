# The main pipeline for default_python
resources:
  pipelines:
    default_python_pipeline:
      name: default_python_pipeline
      catalog: main
      schema: default_python_${bundle.target}
      serverless: true
      libraries:
        - notebook:
            path: ../src/pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
