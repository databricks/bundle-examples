# A sample job for lakeflow_pipelines_python.

resources:
  jobs:
    orchestrate_tpch_lakeflow_pipeline:
      name: orchestrate_tpch_lakeflow_pipeline

      trigger:
        # Run this job every day, exactly one day from the last run; see https://docs.databricks.com/api/workspace/jobs/create#trigger
        periodic:
          interval: 1
          unit: DAYS
        pause_status: PAUSED

      tasks:
        - task_key: refresh_pipeline
          pipeline_task:
            pipeline_id: ${resources.pipelines.tpch_lakeflow_pipeline.id}

      environments:
        - environment_key: default
          spec:
            environment_version: "2"

      #email_notifications:
      #  on_failure:
      #    - your_email@example.com
