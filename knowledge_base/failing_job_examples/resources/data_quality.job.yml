resources:
  jobs:
    data_quality_failure:
      name: failing_job_example_data_quality
      description: Intentionally fails after detecting invalid transaction records.
      tags:
        example: intentional-failure
        failure_type: data-quality
      max_concurrent_runs: 1
      tasks:
        - task_key: validate_transactions
          max_retries: 0
          notebook_task:
            notebook_path: ../src/data_quality.py
