resources:
  schemas:
    schema_faker_bronze:
      catalog_name: ${var.bronze_catalog}
      name: faker
      comment: |
        # Faker Pipeline Schema - Bronze
        This schema contains raw synthetic data from the Faker data source.
      grants:
        - principal: ${workspace.current_user.userName}
          privileges:
            - MANAGE
            - SELECT
            - MODIFY
