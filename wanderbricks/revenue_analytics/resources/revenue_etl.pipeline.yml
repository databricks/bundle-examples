resources:
  pipelines:
    revenue_etl:
      name: Revenue ETL (medallion)
      catalog: dais2026
      schema: ${var.schema}
      serverless: true
      root_path: ../src/revenue_etl
      libraries:
        - glob:
            include: ../src/revenue_etl/transformations/**
