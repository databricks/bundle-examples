# The main pipeline for lakeflow_pipelines_sql

resources:
  pipelines:
    pipelines_sql_etl:
      name: pipelines_sql_etl_nada
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/pipelines_sql_etl"

      libraries:
        - glob:
            include: ../src/pipelines_sql_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
